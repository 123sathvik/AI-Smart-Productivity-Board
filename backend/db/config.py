DB_config = {
    "host": "localhost",
    "user": "root",
    "password": "root",
    "database": "smart_pd_board",
    "charset": "utf8mb4",  # optional but recommended
    "cursorclass": None     # we set this in get_connection()
}
